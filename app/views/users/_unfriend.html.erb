<%= form_for(current_user.friendships.find_by(friend_id: @user.id), method: :delete) do |f| %>
  <div><%= hidden_field_tag :friend, @user.id %></div>
  <%= f.submit "Unfriend", class: "btn btn-danger" %>
<% end %>